# Copy to infra/env/postgres.env (gitignored).
# Must match credentials embedded in ACTION_DESK_DATABASE_URL in backend.env.

POSTGRES_DB=action_desk
POSTGRES_USER=action_desk
POSTGRES_PASSWORD=CHANGE_ME
