# Copy to infra/env/frontend.env (gitignored).
# Client bundle: NEXT_PUBLIC_* is baked at docker build time (see infra/compose/docker-compose.yml build args).
# Keep this aligned with the build arg; document overrides for custom images.

NEXT_PUBLIC_API_BASE_URL=/api
